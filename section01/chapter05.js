// 1. Number Type
// +, -, *, / 가능, 모듈러 연산(%) 가능 
let num1 = 27;
let num2 = 1.5;
let num3 = -23;

let inf = Infinity;
let mInf = -Infinity;

let nan = NaN;

// 2. String Type
// ', " 사용
let myName = "suyeon";
let myLocation = "서울";
let intro = myLocation + myName; // 서울 suyeon

// 템플릿 리터럴 문법
let introMyself = `${myLocation}` + '에서 온 ' + `${myName}` + '입니다.'; // 서울에서 온 suyeon입니다.

// 3. Boolean Type
let isSwitchOn = true;
let isEmpty = false;

// 4. Null Type (아무것도 없다)
// 개발자가 직접 초기화하는 경우 사용
let empty = null;

// 5. Undefined Type
let none; // Undefined